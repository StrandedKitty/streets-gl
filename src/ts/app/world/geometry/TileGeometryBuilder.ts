export default class TileGeometryBuilder {
	private x: number;
	private y: number;

	constructor(x: number, y: number) {
		this.x = x;
		this.y = y;
	}

	public process(data: any) {
		console.log(data);
	}
}
